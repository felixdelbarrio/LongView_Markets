# Ingestion

Ingestion endpoints support ticker sync, universe sync, default universes, portfolio sync, FX, news and daily close jobs. Jobs are persisted in SQLite with provider, status, row counts, warnings and errors.
