# Portfolio Valuation

The valuation engine calculates lots, average cost, market value, realized PnL, unrealized PnL, dividends, fees, taxes, total return, FX and exposure by currency, country and sector.

Every calculated response includes the calculation version and data quality flags such as `stale_price` or `stale_fx`.
