# Generative Prompts

Prompt registry lives in `backend/app/generative/prompt_registry.py`. Prompts are versioned and associated with JSON schemas. Local edits should create a new local version rather than overwriting base prompts.
