# Generative Privacy

Generative context excludes secrets, `.env`, API keys, logs and local paths. `generative_include_sensitive_portfolio_details=false` sends weights and aggregate context instead of detailed absolute amounts.
